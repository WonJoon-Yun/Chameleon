"""Import-safe library modules for the code constructions (surface, color and
fixed-frame masks, BB mechanism enumeration). Bodies unchanged; only root resolution
and cross-imports adjusted for the package location."""
